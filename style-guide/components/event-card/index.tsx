import React from "react";

interface EventCardProps {
  transform_style: object;
  heading: string;
  text: string;
  text2: string;
  bg: any;
}

const EventCard = (props: EventCardProps) => {
  return <div></div>;
};

export default EventCard;
